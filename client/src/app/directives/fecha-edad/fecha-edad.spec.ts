import { FechaEdad } from './fecha-edad';

describe('FechaEdad', () => {
  it('should create an instance', () => {
    const directive = new FechaEdad();
    expect(directive).toBeTruthy();
  });
});
