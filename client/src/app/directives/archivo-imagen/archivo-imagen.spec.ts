import { ArchivoImagen } from './archivo-imagen';

describe('ArchivoImagen', () => {
  it('should create an instance', () => {
    const directive = new ArchivoImagen();
    expect(directive).toBeTruthy();
  });
});
